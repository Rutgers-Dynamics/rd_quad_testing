Testing for quadruped with mjlab.

Ensure that uv is installed.

Clone the repository then

```uv sync```

If you don't have a nvida gpu, the torch dependencies may break. If this is the case, please make sure that the cpu version of torch is specified in the pyproject.tml file.
